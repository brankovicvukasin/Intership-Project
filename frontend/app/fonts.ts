import { Inter } from "next/font/google";

export const inter = Inter({
  weight: "700",
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const inter500 = Inter({
  weight: "500",
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});
